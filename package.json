{
  "name": "disk-cleaner-mcp",
  "version": "0.1.0",
  "description": "A safety-first disk cleanup MCP server for AI agents — scan, classify, and clean junk files with user-controlled confirmation workflows",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "disk-cleaner-mcp": "./dist/index.js"
  },
  "files": [
    "dist/",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "mcp",
    "disk-cleaner",
    "claude-code",
    "codex",
    "disk-space",
    "cleanup",
    "windows",
    "skill",
    "ai-agent",
    "model-context-protocol"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/Cori-anba/disk-cleaner.git"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
